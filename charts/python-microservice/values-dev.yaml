# Dev overrides: cheaper, single replica, no public LB (saves ELB cost while iterating).
replicaCount: 1

service:
  type: ClusterIP

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 150m
    memory: 128Mi
